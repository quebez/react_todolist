# React_ToDoList based on ReduxSimpleStarter

Checkout this repo, install dependencies, then start the gulp process with the following:

```
> git clone https://github.com/quebez/react_todolist.git
> cd react_todolist
> npm install
> npm start
```